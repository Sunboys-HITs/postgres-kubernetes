namePrefix: ci-

resources:
  - ../../base

labels:
  - pairs:
      app.kubernetes.io/instance: ci-postgres
    includeSelectors: true

patches:
  - target:
      kind: PersistentVolumeClaim
      name: postgres-data
    patch: |-
      - op: replace
        path: /spec/resources/requests/storage
        value: 1Gi

secretGenerator:
  - name: postgres-secret
    namespace: infra
    envs:
      - secret.env

generatorOptions:
  disableNameSuffixHash: true
