namePrefix: auth-

resources:
  - ../../base

labels:
  - pairs:
      app.kubernetes.io/instance: auth-postgres
    includeSelectors: true

secretGenerator:
  - name: postgres-secret
    namespace: infra
    envs:
      - secret.env

generatorOptions:
  disableNameSuffixHash: true
