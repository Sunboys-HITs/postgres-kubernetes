namePrefix: main-

resources:
  - ../../base

labels:
  - pairs:
      app.kubernetes.io/instance: main-postgres
    includeSelectors: true

secretGenerator:
  - name: postgres-secret
    namespace: infra
    envs:
      - secret.env

generatorOptions:
  disableNameSuffixHash: true
